import 'package:flutter/material.dart';

class ProductVariationsView extends StatelessWidget {
  const ProductVariationsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

import 'package:flutter/material.dart';

class ProductVariationsViewBody extends StatelessWidget {
  const ProductVariationsViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
